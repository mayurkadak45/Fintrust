server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://ep-super-cloud-a125b71d-pooler.ap-southeast-1.aws.neon.tech/fintrust_v1?user=neondb_owner&password=npg_5DfFPyxb9tud&sslmode=require&channelBinding=require
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: MySecretKeyForFinTrustBankThatIsLongEnoughForHS256Algorithm
  expiration: 3600000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.fintrust.auth: DEBUG
